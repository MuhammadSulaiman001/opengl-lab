Env-Setup:
	Header files:
		C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Auxiliary\VS\include

	Libs:
		C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Auxiliary\VS\lib\x86

	Dlls:
		C:\Windows\System32


	Project → project Properties → configuration properties → Linker → Input → Additional Dependencies → Edit → and past ( GLAUX.lib;GLU32.lib;glut.lib;OPENGL32.lib;glut32.lib) ➔ Ok→ Apply

	Project → project Properties → configuration properties → Linker->System-> SubSystem = Windows ➔ Ok→ Apply

	Project → project Properties → configuration properties → Advanced -> charechter set -> multi-byte

_____________________
Be-aware: Copy-Paste cpp files from other folders will add a reference to them but not a real file duplicate.
_____________________