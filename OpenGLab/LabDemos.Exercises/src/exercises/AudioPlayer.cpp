// optional #include or import statements. These only apply to this file
// imports in the associated module's interface are automatically available to this file

module Exercise;

namespace AudioPlayer
{
	int main()
	{
		// TODO: Open GLFW window, add ImGUI window with 3 buttons:
		// 'Open': when clicked, a file picker is show, allowing the user to select an audio file (ex. .mp3 file),
		// 'Play' and 'Stop': to play and stop the audio
		// Refer to `OpenFileDialog` sample to get started
		return 0;
	}
}
