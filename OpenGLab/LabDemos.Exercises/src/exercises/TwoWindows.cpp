// optional #include or import statements. These only apply to this file
// imports in the associated module's interface are automatically available to this file

module Exercise;

namespace TwoWindows
{
	int main()
	{
		// TODO: Open 2 GLFW windows,
		// the first one titled "WireFrame", with a triangle-fan is drawn in the middle of the window
		// the second one titled "Filled", with a filled triangle-fan is drawn in the middle of the window
		// Use the same vertices array in both exercises
		return 0;
	}
}
