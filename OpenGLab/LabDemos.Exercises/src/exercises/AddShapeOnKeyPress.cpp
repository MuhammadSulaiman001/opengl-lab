// optional #include or import statements. These only apply to this file
// imports in the associated module's interface are automatically available to this file

module Exercise;

namespace AddShapeOnKeyPress
{
	int main()
	{
		// TODOs: 
		// v.1 Open GLFW window, When user presses 'A' key, a shape is added at the center of the window
		// v.2 Open GLFW window, When user presses 'A' key, a new shape is added at the top left of the window, the next shape is added next to the first one, and so on (like a Grid with N rows x M columns)
		return 0;
	}
}
