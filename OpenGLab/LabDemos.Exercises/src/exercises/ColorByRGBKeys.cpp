// optional #include or import statements. These only apply to this file
// imports in the associated module's interface are automatically available to this file

module Exercise;

namespace ColorByRGBKeys
{
	int main()
	{
		// TODO: Open GLFW window with one white shape (ex. triangle) is shown at center
		// When user clicks on 'R' key, the shape becomes Red
		// When user clicks on 'G' key, the shape becomes Green
		// When user clicks on 'B' key, the shape becomes Blue
		return 0;
	}
}
