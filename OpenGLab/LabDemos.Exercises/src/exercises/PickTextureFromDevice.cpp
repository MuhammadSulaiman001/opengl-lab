// optional #include or import statements. These only apply to this file
// imports in the associated module's interface are automatically available to this file

module Exercise;

namespace PickTextureFromDevice
{
	int main()
	{
		// TODO: Open GLFW window, with a white rectangle is shown in the middle of the window + ImGUI window with 2 buttons:
		// 'Open': when clicked, a file picker is show, allowing the user to select an image file (ex. .png file), when file is selected, add it as a texture to the rectangle
		// 'Clear': should clear the texture and reset the rectangle to white.
		// Refer to `OpenFileDialog` sample to get started
		return 0;
	}
}
